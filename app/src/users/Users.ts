/// <reference path="../../../typings/tsd.d.ts" />

module Users {
  'use strict';
  
  // Prepare the 'users' module for subsequent registration of controllers and delegates
  export const ngModule = angular.module('users', [ 'ngMaterial' ]);
  
}
